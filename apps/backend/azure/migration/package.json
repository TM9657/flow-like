{
	"name": "flow-like-azure-migration",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"description": "One-shot `prisma db push` against Azure Database for PostgreSQL Flexible Server as the migration managed identity.",
	"scripts": {
		"migrate": "bun run migrate.ts",
		"typecheck": "bunx tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@azure/identity": "4.13.1",
		"dotenv": "17.2.3",
		"pg": "8.23.0",
		"prisma": "^7.2.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.9.3"
	},
	"trustedDependencies": ["@prisma/engines", "prisma"]
}
