---
interface Props {
  t: (key: string) => string;
}

const { t } = Astro.props;
const useCases = t("24h.usecases.list").split(",");
---

<section class="relative py-28">
  <div class="mx-auto max-w-4xl px-6 lg:px-8 text-center">
    <p class="text-[11px] font-medium tracking-[0.3em] uppercase text-muted-foreground mb-4">
      {t("24h.usecases.tagline")}
    </p>
    <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight mb-14">
      {t("24h.usecases.headline")}
    </h2>

    <div class="flex flex-wrap justify-center gap-3">
      {useCases.map((useCase) => (
        <span class="px-5 py-2.5 text-sm border border-border rounded-full text-muted-foreground hover:text-foreground hover:border-primary/40 transition-colors cursor-default">
          {useCase.trim()}
        </span>
      ))}
    </div>

    <p class="text-sm text-muted-foreground mt-12">
      {t("24h.usecases.other")} <a href="#form" class="text-primary hover:underline">{t("24h.usecases.cta")}</a>
    </p>
  </div>
</section>
