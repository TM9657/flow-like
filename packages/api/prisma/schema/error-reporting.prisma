model ErrorReport {
  id         String   @id
  userId     String?
  method     String
  path       String
  statusCode Int
  publicCode String
  summary    String
  details    Json?
  createdAt  DateTime @default(now()) @db.Timestamptz(3)
  updatedAt  DateTime @updatedAt @db.Timestamptz(3)
}
