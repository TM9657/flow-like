model Node {
  id String @id

  name        String
  description String?
  category    String
  icon        String?

  bitId String @unique

  createdAt DateTime @default(now()) @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @db.Timestamptz(3)
}
