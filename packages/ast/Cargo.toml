[package]
name = "flow-like-ast"
version = "0.1.0"
edition = "2024"
description = "FlowScript: text-domain IR (AST) for flow-like boards — pure language half (model, render, parse, lint, signatures)."

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
flow-like-types-contracts = { path = "../types/contracts", default-features = false, features = ["geometry"] }
